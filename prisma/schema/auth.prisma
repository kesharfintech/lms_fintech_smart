model User {
  id        Int      @id @default(autoincrement())
  name      String
  email     String   @unique
  password  String

  menus     Menu[]

  createdAt DateTime @default(now())
  createdBy String?
  updateBy  String?
  updateAt  DateTime?
}
